<% @peer_evals.each do |pe| %>
  <p><%= pe.milestone %> 
  <p><%= pe.user_id %> 
  <p><%= pe.goals %> <%= pe.quality %> <%= pe.effort %> <%= pe.considerate %> <%= pe.knowledge%> <%= pe.sharing%> 
  <p><%= pe.strength%> 
  <p><%= pe.weakness%> 
  <%= link_to 'See on separate page', peer_eval_path(pe) %>
<% end %>

<p><%= link_to 'Create New Peer Evaluation', "/peer_evals/new" %>
